### что сделал и как проверил

#### бекенд
1. Добавил запрос на получение общих свойств
2. Исправил sql-Запрос получения свойств по cat_prop
в файле `app/public/admin/ajax/property/Refresh_Property_Good.php`

Проверил ручками потыкав и сверив что "Мебель" показывает общие свойства и свои: мебель (id:1) имеет свойства - общие(артикул,комментарий) и свои(где id:1 есть в списке столбца cat_prop таблицы property_s)

#### фронтенд
1. Добавил сбор значений чекбоксов в `app/public/admin/js/main.js`
2. Сохранил старые значения в переменную state и при обновлении формы добавляю эти значения в новую форму `app/public/admin/js/property.js`

### сколько времени фактически потратили на задание;
около 3.5 часов
из них ~1 час на ознакомление с кодом, 1 час на бекенд, 1.5 часа на фронтэнд(большую часть заняло сохранение состояния при ajax)

### использовались ли нейросети; если да — какие.
только для вопросов по фронту. больше использовал документацию js/php.